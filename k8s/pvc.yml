apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: deals-store
spec:
  storageClassName: ""
  volumeName: mysql-pv
  resources:
    requests:
      storage: 1Gi
  accessModes:
    - ReadWriteOnce
